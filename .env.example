# Copy to `.env` and fill in only if running live-model experiments.
# The repository runs fully in mock mode without any of these.

# Anthropic Claude
ANTHROPIC_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Default provider for experiments ("mock", "anthropic", or "openai").
DEFAULT_PROVIDER=mock
