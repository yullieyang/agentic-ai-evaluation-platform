# Core scientific stack (required for the library, experiments, and tests).
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pydantic>=1.10
PyYAML>=6.0
python-dotenv>=1.0
tabulate>=0.9

# Dashboard (required only to run the Streamlit app).
streamlit>=1.30
plotly>=5.9

# Testing.
pytest>=7.0

# Optional live providers (NOT required for mock mode or tests).
# Install only if running live-model experiments.
# anthropic>=0.40
# openai>=1.0
